require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/repair_jobs", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # RepairJob. As you add validations to RepairJob, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      RepairJob.create! valid_attributes
      get repair_jobs_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      repair_job = RepairJob.create! valid_attributes
      get repair_job_url(repair_job)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_repair_job_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      repair_job = RepairJob.create! valid_attributes
      get edit_repair_job_url(repair_job)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new RepairJob" do
        expect {
          post repair_jobs_url, params: { repair_job: valid_attributes }
        }.to change(RepairJob, :count).by(1)
      end

      it "redirects to the created repair_job" do
        post repair_jobs_url, params: { repair_job: valid_attributes }
        expect(response).to redirect_to(repair_job_url(RepairJob.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new RepairJob" do
        expect {
          post repair_jobs_url, params: { repair_job: invalid_attributes }
        }.to change(RepairJob, :count).by(0)
      end

      it "renders a response with 422 status (i.e. to display the 'new' template)" do
        post repair_jobs_url, params: { repair_job: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_content)
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested repair_job" do
        repair_job = RepairJob.create! valid_attributes
        patch repair_job_url(repair_job), params: { repair_job: new_attributes }
        repair_job.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the repair_job" do
        repair_job = RepairJob.create! valid_attributes
        patch repair_job_url(repair_job), params: { repair_job: new_attributes }
        repair_job.reload
        expect(response).to redirect_to(repair_job_url(repair_job))
      end
    end

    context "with invalid parameters" do
      it "renders a response with 422 status (i.e. to display the 'edit' template)" do
        repair_job = RepairJob.create! valid_attributes
        patch repair_job_url(repair_job), params: { repair_job: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_content)
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested repair_job" do
      repair_job = RepairJob.create! valid_attributes
      expect {
        delete repair_job_url(repair_job)
      }.to change(RepairJob, :count).by(-1)
    end

    it "redirects to the repair_jobs list" do
      repair_job = RepairJob.create! valid_attributes
      delete repair_job_url(repair_job)
      expect(response).to redirect_to(repair_jobs_url)
    end
  end
end
